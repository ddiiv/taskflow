{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "taskflow",
      "runtimeExecutable": "bash",
      "runtimeArgs": [
        "-c",
        "export NVM_DIR=\"${NVM_DIR:-$HOME/.nvm}\"; [ -s \"$NVM_DIR/nvm.sh\" ] && . \"$NVM_DIR/nvm.sh\" && nvm use >/dev/null 2>&1; exec npm run dev"
      ],
      "port": 3000,
      "autoPort": true
    }
  ]
}
